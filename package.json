{
  "name": "launchpad-pro-mk3-bitwig-controller",
  "version": "1.1.1",
  "description": "Novation Launchpad Pro Mk3 controller script for Bitwig Studio",
  "main": "dist/Launchpad-Pro-Mk3.control.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run compile",
    "compile": "npx tsc -p tsconfig.json",
    "watch": "npx tsc -p tsconfig.json --watch"
  },
  "author": "slow wild / Simon Heimler",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "typed-bitwig-api": "^12.0.0",
    "typescript": "^4.4.3"
  }
}